# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── Claude Code local settings ────────────────────────────────────────────────
.claude/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
dist/
build/
.Python
.env
.venv
env/
venv/
ENV/

# ── Testing / coverage ────────────────────────────────────────────────────────
.tox/
.coverage
.coverage.*
htmlcov/
.cache
nosetests.xml
coverage.xml
.hypothesis/
.mypy_cache/

# ── Generated / large data files (reproducible from source) ──────────────────
*.npy
*.pickle
*.pkl

# Raw data files that are too large for git (>50 MB)
data/more_raw_twi_chinese_pairs/academic_reverse_pivot.csv

# Regenerated training splits (built by prepare_bidir.py + prepare_bpe.py)
data/twi_chi/train.src
data/twi_chi/train.tgt

# ── Training outputs ──────────────────────────────────────────────────────────
results/
# But keep the metrics log and test output for reproducibility
!results/metrics.jsonl
!results/twi_chi_test.out

# ── Streamlit app model files (large binaries — mount at runtime) ─────────────
streamlit_app/models/*.ckpt
streamlit_app/models/*.pickle
streamlit_app/models/*.pkl
streamlit_app/models/*.model
streamlit_app/models/*.vocab
!streamlit_app/models/.gitkeep
!streamlit_app/models/README.md

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints

# ── Misc packaging ────────────────────────────────────────────────────────────
pip-log.txt
pip-delete-this-directory.txt
*.manifest
*.spec
/site
*.log
*.mo
*.pot
celerybeat-schedule
*.sage.py
.spyderproject
.spyproject
.ropeproject
